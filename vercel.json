{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/works/(.*)", "destination": "/api/works" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
